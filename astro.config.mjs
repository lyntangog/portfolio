import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://lyntangog.github.io',
  base: '/portfolio', // set to '/' if using a <username>.github.io repo
  output: 'static',
});
